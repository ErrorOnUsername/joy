decl MyEnum : enum
{
    VariantA;
    VariantB;
}

decl MyStruct : struct
{
    member_0: i32;
    member_1: f32;
    member_2: cstring;
    member_3: string;
    member_4: usize;
}

decl main : ( a: rawptr )
{
    let a := "haha";
    let b: isize = 10;

    if b == 10 {
        b *= 10;
    } else if b > 10 {
        b = 10;
    } else {
        b -= 1;
    }

    let fact := 2;
    for i in [0..b) {
        fact *= fact;
    }

    while fact > 0 {
        fact--;
    }

    loop { break; }

    println( "Hello, World!" );
}
