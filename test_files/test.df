#load "other_file.df";

decl MyType : struct
{
	my_int:    ilong;
	my_string: string;
	my_thing:  cstring;
	my_other:  ExternalStruct;
}

decl MyEnum : enum
{
	Var1: 10;
	Var2: 11;
	Var3: 2;
}

decl MyUnion : union
{
	VariantA( value: iword );
	VariantB( name: string );
}


decl main : ()
{
	/*

	//Nested block comments!
	/*
		... ... ...
		... ... ...
		... ... ...
	*/

	*/

	let bin_num   := 0b01000001;
	let oct_num   := 0o527;
	let hex_num   := 0xdeadc0de;
	let int_num   := 92834;
	let float_num := 173.97845;

	let a := 1 + 2 / 3 * 4;
	let b := ( 1 + 2 ) * 3;
	let c := a;
	let d := 1;

	if a > b {
		a = b;
	}

	while a > b {
		++a;
		break;
	}

	let x      := 16.0;
	let sqrt_x := sqrt( x );
	printf( "sqrt( %.3f ) => %.3f\n", x, sqrt_x );

	loop {
		a--;
	}

	for i in [0..10) {
		let haha := 0;
		let a    := haha;
		continue;
	}
}
