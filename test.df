#load "other_file.df";

// Structs
decl MyType : struct
{
	my_int:    ilong;
	my_string: string;
	my_thing:  cstring;
}

// Procedures
decl main : ()
{
	// Expression testing
	let a := 1 + 2 / 3 * 4;
	let b := ( 1 + 2 ) * 3;
	let c := a;
	let d := 1;

	// If statements
	if a > b {
		a = b;
	}

	// While statements
	while a > b {
		++a;
		break;
	}

	// Test calling procs from other modules
	let x      := 16.0;
	let sqrt_x := sqrt( x );
	printf( "sqrt( %.3f ) => %.3f\n", x, sqrt_x );

	// Loop statements
	loop {
		a--;
	}

	// For statements (range expressions as well)
	for i in [0..10) {
		let haha := 0;
		let a    := haha;
		continue;
	}
}
